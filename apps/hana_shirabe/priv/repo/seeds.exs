# 用于填充数据库的脚本。您可以这样运行：
#
#     mix run priv/repo/seeds.exs
#
# 在脚本内部，您可以直接读写任何代码库：
#
#     HanaShirabe.Repo.insert!(%HanaShirabe.SomeSchema{})
#
# 我们建议使用带感叹号发 bang 函数（例如 `insert!`、`update!`
# 等），因为如果出现问题，它们将会失败。
